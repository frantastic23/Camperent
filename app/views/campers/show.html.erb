<div class="card-trip">
  <%= image_tag @camper.photo_url %>
  <div class="card-trip-infos">
    <div>
      <h2> <%= @camper.model %></h2>
      <ul>
              <% @camper.description.delete("[]\"").split(',').each_with_index do |item , i| %>
                <p><%= item %></p>
              <% end  %>
      </ul>

    </div>
    <h2 class="card-trip-pricing"><%= @camper.price %>€</h2>
    <img src="https://kitt.lewagon.com/placeholder/users/krokrob" class="card-trip-user avatar-bordered" />
  </div>
</div>

<br>

<div class="container mt-4">
  <div class="row">
    <div class="col-6 m-auto bg-light my-4 shadow p-3 mb-5 bg-body rounded">
      <div class="flat-form mx-4 mt-4">
        <% if user_signed_in? %>
          <div class="md-form md-outline input-with-post-icon datepicker">
            <%= simple_form_for [ @user, @camper, @booking ] do |f| %>

              <%= f.input :date_from, as: :date, placeholder:'select date', :input_html => { :class => 'form_control date_picker', id: "from" } %>
              <%= f.input :date_to, as: :date, placeholder:'select date', :input_html => { :class => 'form_control date_picker', id: "to" } %>
              <br>
              <% if @camper. %>
              <%= f.submit "Book this camper", class: "btn btn-primary" %>
            <% end %>
          </div>
        <% else  %>
          <%= link_to sing_in_path %>
        <% end  %>
      </div>
    </div>
  </div>
  </div>
<div class="md-form md-outline input-with-post-icon datepicker">
  <input placeholder="Select date" type="date" id="example" class="form-control">
</div>

<% if user_signed_in? %>
  <div class="md-form md-outline input-with-post-icon datepicker">
    <%= simple_form_for [ @user, @camper, @booking ] do |f| %>

        <%= f.input :date_from, placeholder:'select date', :input_html => { :class => 'form_control date_picker', id: "example" } %>
        <%# <input placeholder="Select date" type="date" id="example" class="form-control"> %>


      <%= f.submit "Submit review", class: "btn btn-primary" %>
    <% end %>
  </div>
<% end  %>

<form action="">
    <div class="input-group">
      <input type="text" class="form-control" placeholder="04.10.2021">
    </div>
    <div class="input-group mt-2">
      <input type="text" class="form-control" placeholder="04.10.2022">
    </div>
    <input type="submit" value="Book" link_to user_booking_request_path class="btn btn-success mt-2" style="width: 100%">
  </form>

<%# <%= button_to "Book Here", new_user_booking_request_path(@camper) , class:'btn btn-lg' %> %>





<h2>Book now </h2>
<form novalidate="novalidate" class="simple_form search" action="/" accept-charset="UTF-8" method="get"><input name="utf8" type="hidden" value="&#x2713;" />
  <div class="search-form-control form-group">
    <input class="form-control string required" type="text" name="date_from" id="search_query" />
    <button name="button" type="submit" class="btn btn-flat">
      <i class="fas fa-search"></i> Search
    </button>
  </div>
</form>
<!-- input with datepicker -->
